<details>
<summary>🔷 Arista DECOM Manual Process</summary>

#### 1) HEC DC → configlets.yml → Remove decom device entry

```mermaid
flowchart LR
  A([Start]) --> B[Open configlets.yml]
  B --> C{Device entry found?}
  C -->|Yes| D[Delete device lines - sw-hec10-hvv-02a - sw-hec10-hvv-02b]
  C -->|No| H([End])
  D --> E[Save file]
  E --> F[Commit and push if using Git]
  F --> H([End])
```

#### 2) HEC DC → hvv.yml → Search device and remove full config block

```mermaid
flowchart TB
  A([Start]) --> B[Open hvv.yml]
  B --> C{Device id present?}
  C -->|Yes| D[Select entire device block - interfaces vlans mlag evpn etc]
  C -->|No| J([End])
  D --> E[Remove device block]
  E --> F[Validate YAML syntax - yamllint or CI]
  F --> G{Validation ok?}
  G -->|Yes| H[Save commit push]
  G -->|No| I[Fix indentation or keys - revalidate]
  I --> F
  H --> J([End])
```

</details>
